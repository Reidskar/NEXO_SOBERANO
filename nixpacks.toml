[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "deno"]

[phases.install]
cmds = ["pip install --break-system-packages -r requirements_railway.txt"]

[start]
cmd = "python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
