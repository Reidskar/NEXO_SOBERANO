[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["python -m pip install -r requirements_railway.txt"]

[start]
cmd = "uvicorn NEXO_CORE.main:app --host 0.0.0.0 --port $PORT"
